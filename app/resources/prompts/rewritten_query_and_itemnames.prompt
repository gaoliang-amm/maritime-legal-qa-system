历史会话：
{history_text}

当前用户问题：{query}

请根据历史会话和当前问题，提取用户正在询问的法律法规名称（item_names）。
1. 如果用户明确提到了法律法规名称，请提取出来。可能有一个或多个，但不能重复。
2. 如果用户使用了代词（如"这个"、"它"），请结合历史会话指代消解，确定法律法规名称。
3. 如果无法确定法律法规名称，item_names 返回空列表。
4. 请重新改写用户的问题（rewritten_query），使其成为包含法律法规名称的独立完整问题，注意不要超过100个字符！

请直接返回JSON格式结果，格式如下：
{{
    "item_names": ["中华人民共和国海商法", "中华人民共和国海上交通安全法"],
    "rewritten_query": "关于《海商法》中..."
}}